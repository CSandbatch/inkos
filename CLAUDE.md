# CLAUDE.md

@AGENTS.md

The instructions above are canonical and shared with every runtime. This file exists only so
Claude Code loads them; do not maintain a second set of rules here.

Task-specific context:

```bash
corepack pnpm agent:context --scope packages/core --issue 17
```

Then read `.agent/context.json`.
