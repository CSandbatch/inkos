name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-test-and-pack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      # `lint` includes typecheck, public-copy lint, generated docs, diagrams,
      # the capability registry, and the provenance ledger.
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm site:build
      - run: pnpm site:links
      - name: Verify Hermes integration tooling
        run: pnpm agent:test
      - name: Verify publishable tarballs
        shell: bash
        run: |
          set -euo pipefail
          for pkg in packages/core packages/studio packages/cli; do
            pack_dir=$(mktemp -d)
            (cd "$pkg" && npm pack --pack-destination "$pack_dir")
            package_json=$(tar -xOf "$pack_dir"/*.tgz package/package.json)
            if echo "$package_json" | grep -q '"workspace:'; then
              echo "workspace protocol leaked into $pkg tarball"
              exit 1
            fi
          done
      - name: Scan public product text
        shell: bash
        run: |
          if rg -n 'Narcooo|InkOS|inkos|INKOS|[\p{Han}]|â€”|Â·|ä¸' README.md CHANGELOG.md CONTRIBUTING.md SECURITY.md SUPPORT.md PRIVACY.md packages/site packages/studio/src/client .github/ISSUE_TEMPLATE; then
            echo "Former identity, stale ownership, CJK text, or mojibake found in a public surface"
            exit 1
          fi
